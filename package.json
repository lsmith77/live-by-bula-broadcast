{
  "name": "live-by-bula-broadcast",
  "private": true,
  "description": "Broadcast overlays for Live! by BULA \u2014 development tooling only; the project itself has no build step.",
  "scripts": {
    "test": "playwright test --project=overlays",
    "test:ui": "playwright test --project=overlays --ui",
    "shots": "playwright test --project=shots"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
